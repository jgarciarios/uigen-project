Your goal is to update any vulnerable dependencies.

Do the following:
1. Run `npm audit`.
2. Run `npm audit fix` to apply updates
4. Run `npm audit` again to verify that the vulnerabilities have been fixed.
5. Run tests and verify the updates didn't break anything.
